// The minimum size of bounding box to draw a stoke for the polyline
export const STROKE_MIN_PIXEL_SIZE = 7;

// The minimum size of bounding box to fill the polyline
export const FILL_MIN_PIXEL_SIZE = 1;


// The maximum size of bounding box to draw a small point (needed if the polyline is too small on canvas but to sitll draw something)
export const SMALL_POINT_MAX_PIXEL_SIZE = 1;

// The mimimum size of bounding box to draw a small point (needed if the polyline is too small on canvas but to sitll draw something)
export const SMALL_POINT_MIN_PIXEL_SIZE = 0.3;
